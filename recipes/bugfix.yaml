name: bugfix
description: Deterministic debugging context with inspectable read-only steps.
parallel:
  - workspace_context
  - git_context
  - test_context
  - live_logs_query
permissions:
  - filesystem.read
  - git.read
